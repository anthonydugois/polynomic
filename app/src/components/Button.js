import styled from "styled-components";

const Button = styled.button.attrs({
  type: "button",
})`
  padding: 0;
  background: none;
  border: none;
  text-align: center;
  line-height: inherit;
  font-size: inherit;
  color: inherit;
`;

export default Button;
