/* @flow */

export type Matrix4T = [
  number, number, number, number,
  number, number, number, number,
  number, number, number, number,
  number, number, number, number,
]

export type Vector4T = [
  number,
  number,
  number,
  number,
]
